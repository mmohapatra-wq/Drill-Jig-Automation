# Ignore Claude Code related files
.claude/
.mcp.json
CREO_MAPKEY_AUTOMATION_GUIDE.md

# Ignore CSV files
*.csv

# Blind-evaluator artifacts and judge config (secret)
*_eval.json
.bluegpt_judge.json

# Drill-jig index-csys export sidecar (the CSV is covered by *.csv above)
*_index_report.txt

# slotpat-probe recorder output (throwaway trail transcription)
slotpat_recipe.txt

# holepat-probe recorder output (throwaway trail transcription)
holepat_recipe.txt

# csystrf-probe recorder output (throwaway trail transcription)
csystrf_recipe.txt

# fastener-probe read-path diagnostic output (throwaway)
fastener_probe_report.txt

# curved-jig probe outputs (throwaway diagnostics, not committed)
slotplane_probe_report.txt
drilljig3d_probe_report.txt
tangent_plane_probe_report.txt

# fastener layout handoff (per-run artifact, like last_jig_spec.json)
fastener_layout.json

# hole layout handoff (holelayoutinator.cmd standalone artifact)
hole_layout.json